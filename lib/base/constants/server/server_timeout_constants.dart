class ServerTimeoutConstants {
  const ServerTimeoutConstants._();

  static const connectTimeOut = Duration(seconds: 120);
  static const receiveTimeOut = Duration(seconds: 120);
  static const sendTimeOut = Duration(seconds: 120);

}